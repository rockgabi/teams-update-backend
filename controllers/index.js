const auth = require('./auth.controller');
const projects = require('./projects.controller');
const updates = require('./updates.controller');

module.exports = {
    auth,
    projects,
    updates,
}
